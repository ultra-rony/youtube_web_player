name: youtube_web_player
description: "A Flutter package that allows you to seamlessly integrate a native WebView to play YouTube videos. Ideal for apps that require in-app video playback without redirecting to the browser."
version: 0.0.8
repository: https://github.com/ultra-rony/youtube_web_player
homepage: https://github.com/ultra-rony/youtube_web_player

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_inappwebview: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
